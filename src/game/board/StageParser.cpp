#include "StageParser.h"

namespace game::board
{
	std::optional<Board> parseStage(const char* layout)
	{
		// TODO: 実装する(25 マス分読み取れなければ std::nullopt。最後に recalculatePower() を呼ぶ)
		(void)layout;
		return std::nullopt;
	}
} // namespace game::board
